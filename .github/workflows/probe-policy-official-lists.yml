name: Probe PBOC Policy Official Lists

on:
  push:
    branches: [v1.9-public-foundation]
    paths:
      - 'tests/probes/probe_policy_official_lists.py'
      - '.github/workflows/probe-policy-official-lists.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - name: Checkout without third-party action
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git fetch --depth=1 origin "${GITHUB_SHA}"
          git checkout --detach FETCH_HEAD
      - name: Probe official policy lists
        run: python3 tests/probes/probe_policy_official_lists.py
